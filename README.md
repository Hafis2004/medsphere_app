# Doctor Med Sphere App

A production-style Flutter Med Sphere application for doctors with authentication, appointment management, video-call UI, and session notes.

## Features
- Doctor login with Firebase Authentication
- Dashboard with appointment status actions
- Video consultation UI using flutter_webrtc
- Firestore-backed session notes with add/edit/delete
- Profile and logout support
- Responsive Material 3 UI with light/dark theming

## Flutter version
- Flutter 3.38.6
- Dart 3.10.7

## Firebase setup
1. Create a Firebase project at https://console.firebase.google.com/
2. Enable Authentication with Email/Password.
3. Enable Firestore Database.
4. Add Android/iOS/Web apps to the project.
5. Download the configuration files:
   - Android: android/app/google-services.json
   - iOS: ios/Runner/GoogleService-Info.plist
   - Web: lib/firebase_options.dart (generated by FlutterFire CLI)
6. Add the Firebase plugins to the app and run:
   ```bash
   flutterfire configure
   ```

## Packages used
- firebase_core
- firebase_auth
- cloud_firestore
- flutter_webrtc
- flutter_riverpod
- go_router
- permission_handler
- intl
- uuid

## Project structure
- lib/core for shared infrastructure, constants, theme, and services
- lib/features for auth, dashboard, notes, profile, and video call screens
- lib/models, lib/repositories, lib/widgets for domain objects, data access, and shared components

## How to run
```bash
flutter pub get
flutter run
```

## Firestore collections
- appointments
- session_notes

## Authentication
Use the demo credentials:
- Email: doctor@medsphere.app
- Password: doctor123

## How to test video calling
- Run the app on two devices/emulators.
- Sign in on both.
- Open the dashboard and start the video call from one instance.
- The current implementation uses local media streams and demonstrates the camera/mic controls and call lifecycle UI.

## How to test notes
- Open the Notes tab.
- Add a title and description.
- Save, edit, or delete the note.

## What is mocked
- The starter app uses mock/demo appointment and profile data.
- Production WebRTC signaling should be replaced with a real Firebase/Firestore signaling implementation for cross-device peer-to-peer sessions.

## Future improvements
- Implement real WebRTC signaling via Firestore
- Add appointment scheduling and real-time patient updates
- Add test coverage and CI workflows
